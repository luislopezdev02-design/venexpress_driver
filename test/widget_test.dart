import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // Sin pruebas todavía. Este archivo venía de la plantilla por
    // defecto de Flutter (probaba un contador que esta app no tiene),
    // así que se dejó como marcador hasta escribir pruebas reales
    // para las pantallas de Venexpress Repartidor.
  });
}